Instructions for working with this project:

Running:
* `ts-node src/main.ts` to run directly from ts-node

Typechecking:
* `tsc --noEmit` to run the TypeScript typechecker without re-compiling files

Testing:
* No automated tests yet
